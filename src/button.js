// take a str, the button label and retturn the elenemt
/**
 *
 * @param {string} buttonName
 * @returns {Element}
 */
const makeButton = buttonName => {
  return `Button: ${buttonName}`
}

module.exports = makeButton
